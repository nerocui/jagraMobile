import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    horizontalStack: {
        flex: 1,
        flexDirection: 'row',
        alignItems: 'center',
        paddingHorizontal: 5,
    },
    title: {
        fontSize: 18
    },
});

export default  styles;
